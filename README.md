# Advent of Code

My playground for messy advent of code solutions

## Usage

``` shell
# run default year
$ cargo run --release

# run specific year
$ cargo run --release -- 2019

# run all years
$ cargo run --release -- all
```
